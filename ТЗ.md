# Техническое задание (ТЗ)

## 0. Введение и цели

Создать веб‑платформу удалённой IT‑студии полного цикла, включающую:

1. публичную витрину услуг и лид‑формы, 2) личный кабинет клиента (ЛК) с диалогом/чатом с менеджером и управлением заказами, 3) админ‑панель с бэкофис‑инструментами. Данные заказов, диалогов и сущностей хранятся в MongoDB.

**Ключевая бизнес‑особенность:** акцент на демпинговой цене в первые 6 месяцев — UI/UX должен ясно коммуницировать выгоду и сроки акций.

**Стратегические цели:** ускорение онбординга клиента (≤ 3 клика до заявки), прозрачная коммуникация (чат + статус заказа), снижение операционных издержек (шаблоны КП/договоров, пресеты сообщений), контролируемое масштабирование (RBAC, очереди, SLA‑индикаторы).

> 🟨 Маркер консультации: места, помеченные этим значком, требуют согласования с профильным специалистом/агентством (юрист, бухгалтер, ИБ, эквайринг и т.п.).

---

## 1. Роли и доступы (RBAC)

* **Гость**: просмотр сайта, расчёт стоимости (калькулятор), отправка заявки/чата‑без‑регистрации.
* **Клиент**: профиль, проекты/заказы, чат, файлы/документы, счета/оплаты, уведомления.
* **Менеджер**: канбан заказов, диалоги, КП/счета, SLA/эскалации, шаблоны ответов.
* **Эксперт/Инженер**: задачи по заказам, обмен файлами, тайм‑лог, внутр. заметки.
* **Администратор**: управление прайсом/услугами, пользователями, ролями, отчётами, аудит‑логами.
* **Финансы**: счета, акты, оплаты, выгрузки.

Матрица прав (выдержка):

* Клиент: R/W свои **orders**, **messages**, **files**, **invoices** (только просмотр/оплата), **profile**.
* Менеджер: R/W **orders** (в пределах назначенных), **messages**, **quotes**, **invoices** (создание), **tasks**.
* Админ: Full access + **settings**, **price_list**, **catalog**, **users**.

---

## 2. Информационная архитектура и страницы

### 2.1 Публичная часть (маркетинг)

* **Главная**: УТП + «Цены ниже рынка на 15–25% в первые 6 мес.»; CTA: «Получить бесплатную диагностику».
* **Услуги** (категории 1–8 из прайса) с карточками, сравнением «рынок vs наша цена», лейблом «демпинг до ММ.ГГГГ».
* **Калькулятор/конфигуратор**: выбор услуги → параметры → быстрый расчёт/диапазон → CTA «Оставить контакт/стартовать чат».
* **Кейсы/портфолио**: 1–2 примера на направление (MVP: статично).
* **Блог/экспертные статьи** (MVP: лендинг‑заглушка).
* **Контакты/политики**: реквизиты, Политика ПДн, оферта. 🟨 Юр. ревью.

### 2.2 Личный кабинет клиента (после регистрации)

* **Дашборд**: активные заказы, последний диалог, «следующие шаги», статусы, кнопка «Начать чат».
* **Заказы**: список/фильтр; детальная карточка: этапы (канбан‑лента), КП/договор/счета, файлы, участники.
* **Чат** (сквозной): сообщения, вложения, цитирование, быстрые ответы, отметки о прочтении, «/команды».
* **Документы**: счета, акты, договора; статусы «черновик/на подписи/оплачен/архив».
* **Профиль**: компания/ФИО, реквизиты, уведомления, доступы коллег.

### 2.3 Админ‑панель (бэкофис)

* **Заказы (Канбан)**: статусы: New → Discovery → Proposal → In Progress → Review → Done → Archived; KPI‑сводка.
* **Диалоги**: омниканал (веб‑чат, email‑шлюз; позже — Telegram). Маршрутизация, очереди, «незакрытые».
* **Прайс/каталог**: услуги, опции, правила ценообразования, период действия демпинга.
* **Документы**: шаблоны КП/договоров/счетов (переменные), генерация PDF.
* **Платежи/счета**: реестр, статусы, выгрузка CSV. 🟨 Эквайринг/онлайн‑чеки.
* **Пользователи/Роли**: создание, приглашения, блокировки, аудит.
* **Отчёты/Аналитика**: воронка, LTV, маржа по услугам, SLA по чатам/заказам.
* **Настройки**: брендинг, почтовые домены, вебхуки, интеграции.

---

## 3. UX‑принципы и дизайн‑система

* **Простая ориентация**: 2–3 клика до заявки/чата. Sticky CTA, «плавающий» чат‑виджет.
* **Доверие**: блок «Как мы работаем» (5 шагов), цифры SLA/сроков, бейджи «Senior‑качество по цене Junior».
* **Прозрачная цена**: сравнительная таблица «рынок/мы/после 6 мес.». Таймер/бейдж акции.
* **Единый чат**: из карточки услуги, из заказа и из хедера — один и тот же диалог.
* **Системные компоненты**: карточки, таблицы с плотностью, лейблы статусов (цвет+иконка), тосты, пошаговые мастера.
* **Доступность**: контраст ≥ WCAG AA, клавиатурная навигация, aria‑label.
* **Локализация**: RU сначала; структура под i18n.
* **Мобильность**: Mobile‑first для чата, заказов и оплаты.

### 3.1 Ключевые экраны (описание макетов)

* **Главная**: Hero (заголовок+подзаголовок+CTA), сетка услуг (4/8), блок «демпинг», кейсы, FAQ, футер.
* **Каталог услуг**: грид карточек → детальная страница услуги (описание, этапы, цена, «рассчитать» + «начать чат»).
* **Калькулятор**: Stepper (параметры) → итоговая смета (диапазон) → «Отправить контакты/Начать чат».
* **ЛК/Дашборд**: 2 колонки: слева «Мои заказы», справа «Последний диалог/уведомления».
* **Чат**: левая колонка — список диалогов; центр — лента; правая — контекст заказа/файлы/участники.
* **Админ/Канбан**: swimlanes по статусам, карточки с SLA‑цветами, быстрые действия (назначить, ответить, КП).

---

## 4. Бизнес‑логика и процессы

### 4.1 Заказ — жизненный цикл

1. Лид (форма/чат) → 2) Discovery (бриф, оценка) → 3) КП/согласование → 4) Договор/счёт → 5) Работа → 6) Приёмка → 7) Акт/оплата → 8) Отзыв/реферальная программа.

Статусы заказа (enum): `new, discovery, proposal, in_progress, review, done, archived, lost`.

### 4.2 Чат/коммуникации

* Сообщения типа: `text, file, system, quote, checklist, code` (+ приватные заметки для сотрудников).
* Теги сообщений: `urgent, finance, scope, legal` — фильтры и SLA‑маркер.
* Эскалации: «нет ответа N часов» → напоминания, перенос в очередь «Нужен ответ клиента/менеджера».

### 4.3 Ценообразование и демпинг

* Прайс содержит 3 колонки: «Рынок», «Демпинг (1–6 мес.)», «После 6 мес.»; услуга имеет период действия акции и правило показа бейджа.
* Калькулятор применяет коэффициенты (сложность, срочность, стек) и даёт диапазон с пояснениями.

---

## 5. Дата‑модель (MongoDB, коллекции, Mongoose‑схемы)

```ts
// users
{
  _id, email, phone, password_hash, role: 'client'|'manager'|'admin'|'finance'|'engineer',
  profile: { firstName, lastName, companyId, timezone, locale },
  status: 'active'|'invited'|'blocked', createdAt, updatedAt
}

// companies
{ _id, name, inn?: string, kpp?: string, ogrn?: string, address?: string, billing?: {...}, createdAt }

// services
{ _id, slug, title, category, description, features: [string],
  marketPriceRange: { min, max }, priceDump: { min, max, activeUntil }, pricePostDump: { min, max },
  options: [{ key, title, type: 'select'|'number'|'bool', values?: any[] }],
  isActive: true
}

// price_rules (опц.)
{ _id, serviceId, rule: { key, op, value, coef }, period: { from, to } }

// orders
{ _id, clientId, companyId, serviceId, title, status, priority: 1|2|3,
  brief: { answers: any }, estimate: { min, max, currency },
  assignedTo?: userId, participants: [userId],
  files: [fileId], messagesThreadId, checklist: [{text, done}],
  timeline: [{ at, by, action, payload }], createdAt, updatedAt
}

// messages (чат)
{ _id, threadId, orderId?, fromUserId, toUserId?, type, text, files: [fileId],
  meta: { tags: [string], quotedMessageId? }, readBy: [userId], createdAt }

// threads
{ _id, ownerType: 'order'|'lead'|'support', ownerId, lastMessageAt, participants: [userId] }

// invoices
{ _id, orderId, number, amount, currency, status: 'draft'|'issued'|'paid'|'void', pdfFileId?, issuedAt, paidAt }

// payments
{ _id, invoiceId, method: 'bank'|'card'|'sbp'|'other', amount, status, txnId?, raw? }

// files (object storage/minio/s3)
{ _id, filename, size, mime, url, ownerId, linkedTo: { type, id }, virusScanned: true }

// tasks (внутр. работы)
{ _id, orderId, title, assigneeId, status, spentMin, comments: [messageId] }

// notifications
{ _id, userId, type, payload, read: false, createdAt }

// audit_logs
{ _id, actorId, action, entity: {type, id}, diff?, ip, ua, at }
```

Индексы: `messages.threadId+createdAt`, `orders.status+assignedTo`, `threads.lastMessageAt`, `invoices.number` (unique), `users.email` (unique).

---

## 6. API (черновой контракт)

REST (JSON). JWT + refresh, CSRF для веб‑клиента.

**Auth**

* POST `/auth/register` `{ email, password, profile }`
* POST `/auth/login` `{ email, password }` → `{ access, refresh }`
* POST `/auth/refresh`

**Catalog**

* GET `/services` → список
* GET `/services/:slug`
* POST `/calc/:serviceId` `{ options }` → `{ estimate: {min,max}, explanation }`

**Orders & Chat**

* GET `/orders?mine=1&status=in_progress`
* POST `/orders` `{ serviceId, title, brief }`
* GET `/orders/:id`
* PATCH `/orders/:id` `{ status, assignedTo, ... }`
* GET `/threads/:ownerType/:ownerId`
* GET `/messages?threadId=...&cursor=...`
* POST `/messages` `{ threadId, text, files, meta }`

**Docs & Billing**

* POST `/quotes` (генерация КП по шаблону) 🟨 Юр. макет
* GET `/invoices?orderId=...`
* POST `/invoices` `{ orderId, items[] }`
* POST `/payments/create` `{ invoiceId, method }` 🟨 Эквайринг
* POST `/files` (multipart)

**Admin**

* CRUD `/users`, `/price`, `/reports`, `/settings`

Вебхуки: `/webhooks/payments` (успех/фейл) 🟨 Финансы/бухучёт.

---

## 7. UI‑компоненты (ключевые)

* **ChatComposer**: поле ввода, /команды, прикрепление файлов (drag&drop), предпросмотр, цитирование, эмодзи.
* **MessageItem**: аватар, роли (бейдж), время, статусы прочтения, теги, actions (copy, quote, pin).
* **OrderCard**: заголовок, статус, SLA‑индикатор (зел/желт/красн), быстрые кнопки (ответить, КП, счёт).
* **PriceTable**: три колонки цен, бейдж «−20% до ММ.ГГГГ».
* **Stepper/BriefForm**: валидируемые поля + сохранение черновика.
* **DocumentViewer** (pdf.js) и **InvoiceBadge** (статусы).

---

## 8. Нефункциональные требования

* **Безопасность**:

  * RBAC, 2FA (email/OTP, опц.),
  * rate‑limit на авторизацию/загрузки,
  * шифрование секретов,
  * антивирусная проверка файлов (clamd),
  * аудит‑лог действий,
  * Политика ПДн/ДПО. 🟨 Юр./ИБ ревью.
* **Производительность**: TTFB < 300 мс (кеш), чат — поллинг 3–5 с или WebSocket; N+1 индексы.
* **Надёжность**: ежедневные бэкапы Mongo (полные + PITR), версионирование файлов, 99.5% аптайм (MVP).
* **Совместимость**: современные Chromium/Firefox/Safari (последние 2 версии), мобильные iOS/Android ≥ 14/10.
* **Логи/Метрики**: request‑лог, бизнес‑события (создан заказ, выслан счёт, оплата), дашборды SLA.

---

## 9. Интеграции (этапно)

* **Почта**: вход/выход через транзакционный SMTP; привязка к тредам.
* **Платежи**: эквайринг картой/банковский счёт/СБП (приём средств, вебхуки). 🟨 Финансы/54‑ФЗ.
* **Мессенджеры** (этап 2): Telegram‑бот → тот же тред.
* **Хранилище**: MinIO/S3‑совместимое, пресайнд‑URL.

---

## 10. Аналитика и события

* Ключевые события: `lead_created, chat_started, order_created, quote_sent, invoice_issued, invoice_paid, order_done`.
* Воронки: лид → заказ → КП → счёт → оплата → отзыв.
* Отчёты: по услугам, по менеджерам, среднее время ответа, средний чек.

---

## 11. Тестирование и качество

* Unit для сервисов/моделей; e2e для основных флоу (регистрация → заказ → чат → счёт); визуальные снапшоты для UI‑компонентов;
* Нагрузочные: чат 100 одновременных тредов, 10 сообщений/мин/тред.

---

## 12. Деплой и окружения

* Стек: **Node.js/NestJS** API, **Next.js/React** фронт, **MongoDB**; брокер (опц. BullMQ/Redis) для очередей; MinIO/S3 для файлов.
* Контейнеризация: Docker Compose/K8s (позже). CI/CD: GitHub Actions + автодеплой на staging/prod.
* Окружения: dev/staging/prod, .env‑секреты (Vault/варс).

---

## 13. MVP‑объём (v0.1 → 6–8 недель)

1. Публичный сайт: Главная, Каталог, Карточка услуги, Форма заявки, Калькулятор (лайт), Политики.
2. ЛК клиента: Дашборд, Заказы (список/деталь), Чат (веб‑виджет), Документы (просмотр), Профиль.
3. Админка: Канбан заказов, Диалоги, Прайс (простая матрица), Счета (черновик), Пользователи, Настройки базовые.
4. Бэкенд/БД: схемы, индексы, RBAC, файлы, уведомления e‑mail, аудит‑лог.

**Out of scope для MVP:** мессенджеры, онлайн‑оплата, блог/кейсы CMS, продвинутые отчёты, телеметрия в глубину.

---

## 14. Риски и допущения

* Правовые тексты (оферта, политика ПДн, Договор) — 🟨 юр. ревью перед публикацией.
* Эквайринг/онлайн‑чек (54‑ФЗ) — 🟨 консультация с платёжным провайдером/бухгалтером.
* Хранение ПДн и файлов с конфиденциальной инфой — 🟨 ИБ‑ревью (классификация, DPIA, VPN, MFA).

---

## 15. Бэклог задач (декомпозиция)

### Эпик A: Публичная витрина

A1. Главная (Hero, УТП, демпинг‑бейдж, CTA)
A2. Каталог услуг (карточки, фильтры)
A3. Карточка услуги (описание, этапы, цены, CTA)
A4. Калькулятор (лайт)
A5. Заявка/лид‑форма + email нотификация

### Эпик B: ЛК клиента

B1. Регистрация/логин, восстановление
B2. Дашборд
B3. Список/деталь заказа
B4. Чат (WebSocket/долгий поллинг)
B5. Документы (просмотр PDF)
B6. Профиль/реквизиты

### Эпик C: Админка

C1. Канбан заказов + назначение
C2. Диалоги (очереди, фильтры, «молчание N часов»)
C3. Прайс/каталог (CRUD)
C4. Счета (черновик) + PDF шаблон
C5. Пользователи/роли (инвайты)
C6. Настройки (брендинг, домены)

### Эпик D: Тех‑платформа

D1. Схемы Mongo + индексы
D2. Файлы (MinIO) + антивирус
D3. E‑mail нотификации, SPF/DKIM/DMARC 🟨
D4. Аудит‑лог
D5. Бэкапы + restore‑плейбуки

---

## 16. Таблицы статусов и меток

* **Статусы заказа:** new, discovery, proposal, in_progress, review, done, archived, lost
* **Приоритет:** high/medium/low
* **Теги сообщений:** urgent, finance, scope, legal
* **SLA ответа (менеджер):**

  * New: 15 мин (раб. время)
  * Discovery/Proposal: 2 ч
  * In Progress: 4 ч

---

## 17. Визуальные ориентиры (гайд без макетов)

* Цвета: базовая палитра нейтральная (серый/графит) + акцент фирменный (например, синий/бирюзовый). Ошибки — красный; успех — зелёный; предупреждение — янтарный.
* Типографика: заголовки 28/22/18; текст 16; плотность строк 1.5; иконки 16/20.
* Карточки: закругления 12–16 px, тень мягкая; отступы 12–20.

---

## 18. Приложение: Сопоставление услуг и UI‑блоков

* Инфраструктура/поддержка → карточка услуги + калькулятор «рабочие места/мес».
* IP‑телефония → мастер брифа (линиии, номера, интеграции CRM).
* Аудит/регуляторы → чек‑лист входных данных, бейдж 🟨 «требуется консультация».
* Разработка ПО → бриф стек/модули/интеграции, оценка T&M/фикс.
* Видеонаблюдение → разделение «железо/работы», калькулятор.
* DevOps → почасовая ставка, капа команды, SLO формулировки.
* Комплексный аудит ИТ/ИБ → шаблон КП, список контролей.
* Интеграция ИИ/BI → пресеты (чат‑бот/аналитика), демо‑скрины (позже CMS).

---

## 19. Критерии приёмки (MVP)

* Лид генерируется ≤ 3 клика с главной и попадает в админ‑канбан.
* Клиент видит один и тот же тред чата в ЛК и в карточке заказа.
* Менеджер может создать КП/счёт (черновик PDF) из карточки заказа.
* Прайс рендерит три колонки цен с бейджем демпинга.
* Логи действий сохраняются, файлы проходят антивирус‑чек.

---

## 20. Дальнейшее (v1.1+)

* Онлайн‑оплаты (эквайринг/СБП), интеграция с 1С/моим складом, Telegram‑бот, CMS для блога/кейсов, продвинутые отчёты, SLA‑алерты в мессенджеры, гостевой доступ в проект.
